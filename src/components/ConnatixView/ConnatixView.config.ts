import ConnatixViewInterface from './ConnatixView.interface';

export const embeddedUrl = `https://elements.connatix.com/extension?wordpress=true&t=`;

export interface ConnatixConfig {
    config: ConnatixViewInterface
}
